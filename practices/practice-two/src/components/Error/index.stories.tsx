import { Meta, Story } from '@storybook/react';

// Component
import { Error } from 'components';

export const Template: Story = () => <Error />;

export default {
  title: 'Practice/components/Error',
  component: Error,
} as Meta;
